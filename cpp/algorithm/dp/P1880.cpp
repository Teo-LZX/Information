#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> arr(n);
    for(int i=0;i<n;i++){
        cin >> arr[i];
    }
    vector<vector<int>> dp(n, vector<int>());
    for(int i=0;i<dp.size();i++){
        for(int j=0;j<dp.size();j++){
            for(int k)
        }
    }
}